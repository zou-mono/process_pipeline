<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <Style x:Key="DefaultTextBlockStyle" TargetType="TextBlock">
    <Setter Property="FontFamily" Value="Segoe UI, Microsoft YaHei" />
    <Setter Property="FontSize" Value="12" />
  </Style>
  <Style x:Key="DefaultButtonStyle" TargetType="Button">
    <Setter Property="FontFamily" Value="Segoe UI, Microsoft YaHei" />
    <Setter Property="FontSize" Value="14" />
  </Style>
  <Style x:Key="DefaultLabelStyle" TargetType="Label">
    <Setter Property="FontFamily" Value="Segoe UI, Microsoft YaHei" />
    <Setter Property="FontSize" Value="9" />
  </Style>

  <!-- 全局圆角按钮 -->
  <Style x:Key="RoundButton" TargetType="Button">
    <Setter Property="Background" Value="#2196F3"/>
    <Setter Property="Foreground" Value="White"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border Background="{TemplateBinding Background}" 
                  CornerRadius="5" 
                  SnapsToDevicePixels="True">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
          </Border>
          <ControlTemplate.Triggers>
            <!-- 鼠标悬停变色 -->
            <Trigger Property="IsMouseOver" Value="True">
              <Setter Property="Background" Value="#005A9E"/>
            </Trigger>
            <!-- 鼠标按下变色 -->
            <Trigger Property="IsPressed" Value="True">
              <Setter Property="Background" Value="#004578"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- 之前的 CAD 深色按钮（全局复用） -->
  <Style x:Key="CadFlatButton" TargetType="Button">
    <!-- 默认状态：背景透明，没有边框，文字浅色 -->
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="Foreground" Value="#F0F0F0"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="Padding" Value="8,4"/>
    <Setter Property="Margin" Value="2,2,2,2"/>
    <Setter Property="Cursor" Value="Hand"/>

    <!-- 重写按钮的内部结构（剥离 Windows 默认的 3D 凸起效果） -->
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border Background="{TemplateBinding Background}" CornerRadius="2">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
          </Border>

          <!-- 触发器：定义鼠标悬停和按下的交互效果 -->
          <ControlTemplate.Triggers>
            <!-- 鼠标悬停时：背景变成浅灰色 (CAD 经典 Hover 效果) -->
            <Trigger Property="IsMouseOver" Value="True">
              <Setter Property="Background" Value="#505050"/>
            </Trigger>
            <!-- 鼠标按下时：背景变成 CAD 经典的主题蓝 -->
            <Trigger Property="IsPressed" Value="True">
              <Setter Property="Background" Value="#005A9E"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!--<Style x:Key="ToolBarStyle" TargetType="ToolBar">
    <Setter Property="Height" Value="{StaticResource ToolbarHeight}"/>
    <Setter Property="MinHeight" Value="{StaticResource ToolbarHeight}"/>
    <Setter Property="MaxHeight" Value="{StaticResource ToolbarHeight}"/>
    <Setter Property="Background" Value="{StaticResource Color.DataGrid.Panel.BackgroundColor}"/>
    <Setter Property="BorderBrush" Value="{StaticResource Color.DataGrid.GridLine}"/>
    <Setter Property="BorderThickness" Value="0,0,0,1"/>
    <Setter Property="Padding" Value="8,4"/>
  </Style>

  <Style x:Key="StatusBarStyle" TargetType="StatusBar">
    <Setter Property="Height" Value="{StaticResource StatusBarHeight}"/>
    <Setter Property="MinHeight" Value="{StaticResource StatusBarHeight}"/>
    <Setter Property="MaxHeight" Value="{StaticResource StatusBarHeight}"/>
    <Setter Property="Background" Value="{StaticResource Color.DataGrid.Panel.BackgroundColor}"/>
    <Setter Property="BorderBrush" Value="{StaticResource Color.DataGrid.GridLine}"/>
    <Setter Property="BorderThickness" Value="1,1,1,0"/>
  </Style>

  <Style x:Key="TreeViewStyle" TargetType="TreeView">
    <Setter Property="VirtualizingStackPanel.IsVirtualizing" Value="True"/>
    <Setter Property="VirtualizingStackPanel.VirtualizationMode" Value="Recycling"/>
    <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
    <Setter Property="Background" Value="{StaticResource Color.DataGrid.Panel.BackgroundColor}"/>
    <Setter Property="BorderBrush" Value="{StaticResource Color.DataGrid.GridLine}"/>
    <Setter Property="BorderThickness" Value="0"/>
  </Style>

  <Style x:Key="DataGridStyle" TargetType="DataGrid">
    <Setter Property="EnableRowVirtualization" Value="True"/>
    <Setter Property="EnableColumnVirtualization" Value="True"/>
    <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True"/>
    <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling"/>
    <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
    <Setter Property="AutoGenerateColumns" Value="False"/>
    <Setter Property="IsReadOnly" Value="True"/>
    <Setter Property="HeadersVisibility" Value="Column"/>
    <Setter Property="GridLinesVisibility" Value="Horizontal"/>
    <Setter Property="RowHeaderWidth" Value="0"/>
    <Setter Property="Background" Value="{StaticResource Color.DataGrid.Panel.BackgroundColor}"/>
    <Setter Property="BorderBrush" Value="{StaticResource Color.DataGrid.GridLine}"/>
    <Setter Property="BorderThickness" Value="0"/>
  </Style>

  <Style x:Key="SplitterStyle" TargetType="GridSplitter">
    <Setter Property="Width" Value="{StaticResource SplitterWidth}"/>
    <Setter Property="Background" Value="{StaticResource Color.DataGrid.Panel.BackgroundColor}"/>
    <Setter Property="HorizontalAlignment" Value="Stretch"/>
    <Setter Property="VerticalAlignment" Value="Stretch"/>
    <Setter Property="ShowsPreview" Value="True"/>
  </Style>

  <Style x:Key="SplitterToggleButtonStyle" TargetType="ToggleButton">
    <Setter Property="Width" Value="18"/>
    <Setter Property="Height" Value="48"/>
    <Setter Property="Background" Value="{StaticResource Color.DataGrid.Panel.BackgroundColor}"/>
    <Setter Property="BorderBrush" Value="{StaticResource Color.DataGrid.GridLine}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Foreground" Value="{StaticResource Color.DataGrid.Text.Foreground}"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ToggleButton">
          <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="9">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>-->

</ResourceDictionary>