<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:WpfSplitTest.Controls">

  <Style TargetType="{x:Type local:SplitToggleGrid}">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type local:SplitToggleGrid}">
          <Grid ClipToBounds="True">
            <Grid.ColumnDefinitions>
              <ColumnDefinition x:Name="PART_LeftCol" Width="320" MinWidth="0"/>
              <ColumnDefinition x:Name="PART_SplitterCol" Width="10"/>
              <ColumnDefinition x:Name="PART_RightCol" Width="*" MinWidth="120"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderBrush="#DDDDDD" BorderThickness="0,0,1,0">
              <ContentPresenter x:Name="PART_LeftHost"
                                              Content="{TemplateBinding LeftContent}" />
            </Border>

            <!-- splitter + handle -->
            <Grid Grid.Column="1" Background="#F3F3F3">
              <GridSplitter x:Name="PART_Splitter"
                                          Background="Transparent"
                                          HorizontalAlignment="Stretch"
                                          VerticalAlignment="Stretch"
                                          ResizeDirection="Columns"
                                          ResizeBehavior="PreviousAndNext"
                                          ShowsPreview="False"
                                          Cursor="SizeWE"/>

              <ToggleButton x:Name="PART_Toggle"
                                          Width="10"
                                          Height="52"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          BorderThickness="0"
                                          Background="#E8E8E8"
                                          Foreground="#333"
                                          Content="◀"
                                          Cursor="Hand"/>
            </Grid>

            <Border Grid.Column="2">
              <ContentPresenter x:Name="PART_RightHost"
                                              Content="{TemplateBinding RightContent}" />
            </Border>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>

